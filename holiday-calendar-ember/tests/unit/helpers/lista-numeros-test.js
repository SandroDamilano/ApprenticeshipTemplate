
import { listaNumeros } from 'holiday-calendar-ember/helpers/lista-numeros';
import { module, test } from 'qunit';

module('Unit | Helper | lista numeros');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = listaNumeros([42]);
  assert.ok(result);
});

